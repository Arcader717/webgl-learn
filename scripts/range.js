function setupSlider(id, min, max, step) {
  const ui = document.querySelector("#ui");
  const slider = document.querySelector("#" + id);
  if (slider == null) {
    throw ("Slider '" + id + "' doesn't exist.")
  }
  let 
  let label = document.createElement("div");
  let 
}
